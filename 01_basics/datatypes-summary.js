// primitive data types :
// types : 7 -- bigInt, string, number, boolean, symbol, undefined, null 

      // reference or non- primitive data types
      // arrays, functions and Objects 

      let myfunctions = function name(params) {
        name = sami 
        age = 20
        sex = male
        console.log(name);
        console.log(age);
      }
      console.log("myfunctions");

      const age = 99
      let name;
      let job = null
      console.log(typeof age);
      console.log(typeof name);

      // read official documentation for better understanding 



      //  +++++++++++++++++ Memory in JS +++++++++++++++

      // there are two types of memory in js 
      // heap and another one is stack memory
      heap memory is used for 
